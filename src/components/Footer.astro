---
import { t } from '../i18n/utils';
import type { Locale } from '../i18n/utils';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const prefix = locale === 'zh' ? '/zh' : '';

const policyLinks = [
  { label: t(locale, 'footer.privacy'), href: `${prefix}/privacy-policy` },
  { label: t(locale, 'footer.cookies'), href: `${prefix}/cookie-policy` },
  { label: t(locale, 'footer.aml'), href: `${prefix}/aml-policy` },
  { label: t(locale, 'footer.ctf'), href: `${prefix}/counter-terrorism-financing` },
];

const year = new Date().getFullYear();
---

<footer class="bg-brand-footer dark:bg-brand-navy2 text-brand-dark dark:text-brand-light">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <!-- Company Info -->
      <div>
        <img src="/images/general/kilc-logo-colour-transparent.png" alt="KILC" class="h-36 w-auto mb-4" />
        <p class="text-gray-600 text-sm leading-relaxed">
          Keane International Legal Consultancy LLP
        </p>
        <p class="text-gray-500 text-sm mt-2">
          {t(locale, 'footer.address')}
        </p>
        <a href="mailto:info@kilc.co.uk" class="text-brand-primary hover:text-brand-secondary text-sm mt-1 inline-block transition-colors">
          {t(locale, 'footer.email')}
        </a>
      </div>

      <!-- Quick Links -->
      <div>
        <h3 class="text-sm font-semibold uppercase tracking-wider mb-4 text-brand-dark">
          {locale === 'zh' ? '快速链接' : 'Quick Links'}
        </h3>
        <ul class="space-y-2">
          <li><a href={`${prefix}/about`} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.about')}</a></li>
          <li><a href={`${prefix}/services`} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.services')}</a></li>
          <li><a href={`${prefix}/fees`} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.fees')}</a></li>
          <li><a href={locale === 'zh' ? '/zh/contact-us' : '/contact'} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.contact')}</a></li>
          <li>
            <a href="https://xos.kilc.co.uk" target="_blank" rel="noopener noreferrer" class="text-gray-600 hover:text-brand-primary text-sm transition-colors">
              {locale === 'zh' ? '打开 XOS' : 'Open XOS'} &rarr;
            </a>
          </li>
          <li>
            <a href="https://gs.kilc.co.uk" target="_blank" rel="noopener noreferrer" class="text-gray-600 hover:text-brand-primary text-sm transition-colors">
              {locale === 'zh' ? '打开 Guardian Shield' : 'Open Guardian Shield'} &rarr;
            </a>
          </li>
          <li><a href={`${prefix}/blogs`} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">{t(locale, 'nav.blog')}</a></li>
        </ul>
      </div>

      <!-- Policies & WeChat -->
      <div>
        <h3 class="text-sm font-semibold uppercase tracking-wider mb-4 text-brand-dark">
          {locale === 'zh' ? '法律信息' : 'Legal'}
        </h3>
        <ul class="space-y-2">
          {policyLinks.map((link) => (
            <li>
              <a href={link.href} class="text-gray-600 hover:text-brand-dark text-sm transition-colors">
                {link.label}
              </a>
            </li>
          ))}
          <li>
            <button
              id="cookie-settings-btn"
              class="text-gray-600 hover:text-brand-dark text-sm transition-colors cursor-pointer bg-transparent border-none p-0"
            >
              {t(locale, 'footer.cookieSettings')}
            </button>
          </li>
        </ul>
        <script is:inline>
          document.getElementById('cookie-settings-btn')?.addEventListener('click', function () {
            window.dispatchEvent(new CustomEvent('kilc:reset-consent'));
          });
        </script>

        <!-- WeChat QR -->
        <div class="mt-6">
          <p class="text-sm text-gray-500 mb-2">
            {locale === 'zh' ? '微信联系' : 'WeChat'}
          </p>
          <img
            src="/images/team/keane-wechat-code.jpg"
            alt="WeChat QR Code"
            class="w-24 h-24 rounded-lg"
            loading="lazy"
          />
        </div>
      </div>
    </div>

    <!-- Partner Logos & Copyright -->
    <div class="mt-12 pt-8 border-t border-gray-300">
      <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
        <div class="flex items-center gap-6">
          <img src="/images/general/british-council-logo.png" alt="British Council" class="h-8 w-auto opacity-60" loading="lazy" />
          <img src="/images/general/ico-logo.png" alt="ICO" class="h-8 w-auto opacity-60" loading="lazy" />
          <img src="/images/general/lanpeng-logo.png" alt="蓝鹏律师事务所" class="h-8 w-auto max-w-[186px] opacity-60" loading="lazy" />
        </div>
        <p class="text-gray-500 text-sm">
          &copy; {year} KILC. {t(locale, 'footer.rights')}
        </p>
      </div>
    </div>
  </div>
</footer>
